[build]
  command = "npm run build"
  publish = "build"

# Si ya tienes redirects para SPA, déjalos igual
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Activa el Edge Function en la ruta /hls/*
[[edge_functions]]
  function = "hls-proxy"
  path = "/hls/*"
