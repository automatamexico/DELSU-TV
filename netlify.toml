[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (keeps client-side routes working)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
