[build]
  command = "npm run build"
  publish = "build"

# Proxy HLS: este edge function atiende /hls/* ANTES que el redirect de SPA
[[edge_functions]]
  function = "hls-proxy"
  path = "/hls/*"

# SPA: tus rutas de React
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
